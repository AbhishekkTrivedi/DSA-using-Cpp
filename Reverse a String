string reverseWord(string str){

    int n= str.length();
    int i= 0;
    int j=  n-1;
    
    for (int k= 0; k< n/2; k++)
    {
      char temp = str[i];
      str[i]=str[j];
      str[j]=temp;
      i++;
      j--;
    };
    return str;
}
